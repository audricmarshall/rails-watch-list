<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# favicon %>
    <%= favicon_link_tag 'favicon.png' %>
  </head>

  <body>
    <%# rajout video en background avec loop %>
  <video autoplay loop muted playsinline id="bg-video">
    <source src="<%= asset_path('bg_cinema.mp4') %>" type="video/mp4">
  </video>
  <div class="bg-overlay"></div>
  <!-- navbar -->
    <nav class="navbar navbar-expand-lg navbar-light navbar-lewagon">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="<%= root_path %>">
          <%= image_tag "favicon.png", alt: "Logo", class: "me-2" %>
          <span class="fw-bold">Audric's Watchlist</span>
          </a>
            <div class="ms-auto d-flex gap-3 align-items-center">
               <%= link_to "My lists", lists_path, class: "btn-ghost" %>
               <%= link_to "New list", new_list_path, class: "btn btn-success " %>
               </div>
            </div>
      </div>
    </nav>

    <!-- alert flash si fail ou succces sur les notice et alert dans les controllers avec ternaire -->
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key == "notice" ? "success" : "danger" %>">
        <%= value %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
